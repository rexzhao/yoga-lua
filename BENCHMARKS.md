# Benchmarks

Run with:

```sh
lua benchmarks/run.lua
```

Latest local run:

```text
layout benchmark (20 iterations)
  100 nodes: total 0.0470s, avg 0.002350s, root 640x294
 1000 nodes: total 0.4530s, avg 0.022650s, root 640x2814
 5000 nodes: total 2.2340s, avg 0.111700s, root 640x14014
virtual list jumps (2000 jumps): total 0.8120s, avg 0.000406s, rendered avg 15.0, visible avg 15.0
```
