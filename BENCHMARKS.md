# Benchmarks

Run with:

```sh
lua benchmarks/run.lua
```

Latest local run:

```text
layout benchmark (20 iterations)
  100 nodes: total 0.0470s, avg 0.002350s, root 640x294
 1000 nodes: total 0.4380s, avg 0.021900s, root 640x2814
 5000 nodes: total 2.0780s, avg 0.103900s, root 640x14014
```
